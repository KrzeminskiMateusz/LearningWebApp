@page
@using Microsoft.AspNetCore.Http
@using ContosoUniversity.Models
@model IndexModel
@{
    ViewData["Title"] = "Session Sample";
}

<h1>@ViewData["Title"]</h1>

<p>Property data</p>
<p><b>Name:</b> @Model.SessionInfo_Name <b>Age:</b> @Model.SessionInfo_Age</p>

<p>Session data</p>
<p>
    <b>Name:</b> @HttpContext.Session.GetString(IndexModel.SessionKeyName)
    <b>Age:</b>
    @HttpContext.Session.GetInt32(IndexModel.SessionKeyAge).ToString()
</p>


<p>Value: @Model.SessionInfo_MiddlewareValue</p>
<p>Current Time: @Model.SessionInfo_CurrentTime</p>
<p>Session Time: @Model.SessionInfo_SessionTime</p>
<p>Name: @Model.SessionInfo_Name</p>
<p>Age: @Model.SessionInfo_Age</p>

<p>--------------------------------------------------</p>

@("<span>Hello World</span>")

<br />

@Html.Raw("<span>Hello World</span>")


@{
    var inCSharp = true;
    <p>Now in HTML, was in C# @inCSharp</p>
}

@using (Html.BeginForm())
{
    <div>
        Email: <input type="email" id="Email" value="">
        <button>Register</button>
    </div>
}

@using System.IO
@{
    var dir = Directory.GetCurrentDirectory();
}
<p>@dir</p>

<address>
    <strong>Support:</strong><email>Support</email><br />
    <strong>Marketing:</strong><email>Marketing</email>
</address>


<p bold>Use this area to provide additional information.</p>
<bold>Is this bold?</bold>

@{
    WebsiteContext webContext = new WebsiteContext
    {
        Version = new Version(1, 3),
        CopyrightYear = 1638,
        Approved = true,
        TagsToShow = 131
    };
}

<website-information info="webContext"/>

<div co>
    <h3>Information about our website (outdated):</h3>
    <website-information info="Model.WebContext"/>
    <div condition="@Model.WebContext.Approved">
        <p>
            This website has <strong surround="em">@Model.WebContext.Approved</strong> been approved yet.
            Visit www.contoso.com for more information.
        </p>
    </div>
</div>

<p>Visit us at http://docs.asp.net or at www.microsoft.com</p>

<p>----------------------------------------------</p>
